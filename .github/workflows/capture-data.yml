name: Capture Data

# DEPRECATED (M3): superseded by tools/data-engineering/capture-agent.js + spool.
# Local launchd DE agent is the only capture path. Do not re-enable — the old
# fire-and-forget capture bypassed the spool and dual-wrote into git.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: echo "capture-data.yml DEPRECATED (M3) — superseded by tools/data-engineering/capture-agent.js + spool. Do not re-enable."
